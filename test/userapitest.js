'use strict';

const assert = require('chai').assert;

/**
 * This is a simple test, guaranteed to succeed
 */
suite('User API tests', function () {

  test('get users', function () {

    assert.equal(1, 1);   //try 'assert.equal(1, 2)' to guarantee a failed test

  });
});
